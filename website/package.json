{
  "name": "website",
  "version": "0.0.1",
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "@fontsource-variable/fraunces": "^5.2.9",
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource-variable/source-serif-4": "^5.2.9",
    "astro": "^6.1.7"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "bun run scripts/copy-talk-assets.ts && bun run scripts/copy-headshots.ts && bun run scripts/fetch-credly.ts && bun run scripts/generate-og.ts",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "lint": "biome check .",
    "format": "biome format --write .",
    "migrate:talks": "bun run scripts/migrate-talks.ts",
    "migrate:talks:dry": "bun run scripts/migrate-talks.ts --dry",
    "sync:credly": "bun run scripts/fetch-credly.ts",
    "test": "bun test"
  },
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@resvg/resvg-js": "^2.6.2",
    "gray-matter": "^4.0.3",
    "satori": "^0.26.0",
    "yaml": "^2.8.3"
  }
}
